version: 0
allowPullRequests: public
metadata:
  name: "Mozilla RelEng"
  description: "Mozilla RelEng Services"
  owner: "{{ event.head.user.email }}"
  source: "{{ event.head.repo.url }}"
tasks:

# --- elm_common_example (master) ---

  - metadata:
      name: "elm_common_example"
      description: "Test, build and deploy elm_common_example"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/lib/elm_common/example"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-elm_common_example-*
      - assume:hook-id:project-releng/services-master-elm_common_example-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "elm_common_example"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- elm_common_example (staging) ---

  - metadata:
      name: "elm_common_example"
      description: "Test, build and deploy elm_common_example"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/lib/elm_common/example"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-elm_common_example-*
      - assume:hook-id:project-releng/services-staging-elm_common_example-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "elm_common_example"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-archiver (master) ---

  - metadata:
      name: "mozilla-releng-archiver"
      description: "Test, build and deploy mozilla-releng-archiver"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_archiver"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-releng-archiver-*
      - assume:hook-id:project-releng/services-master-mozilla-releng-archiver-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-archiver"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-archiver (staging) ---

  - metadata:
      name: "mozilla-releng-archiver"
      description: "Test, build and deploy mozilla-releng-archiver"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_archiver"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-releng-archiver-*
      - assume:hook-id:project-releng/services-staging-mozilla-releng-archiver-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-archiver"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-clobberer (master) ---

  - metadata:
      name: "mozilla-releng-clobberer"
      description: "Test, build and deploy mozilla-releng-clobberer"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_clobberer"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-releng-clobberer-*
      - assume:hook-id:project-releng/services-master-mozilla-releng-clobberer-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-clobberer"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-clobberer (staging) ---

  - metadata:
      name: "mozilla-releng-clobberer"
      description: "Test, build and deploy mozilla-releng-clobberer"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_clobberer"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-releng-clobberer-*
      - assume:hook-id:project-releng/services-staging-mozilla-releng-clobberer-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-clobberer"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_frontend (master) ---

  - metadata:
      name: "releng_frontend"
      description: "Test, build and deploy releng_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_frontend-*
      - assume:hook-id:project-releng/services-master-releng_frontend-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_frontend (staging) ---

  - metadata:
      name: "releng_frontend"
      description: "Test, build and deploy releng_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_frontend-*
      - assume:hook-id:project-releng/services-staging-releng_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_frontend (production) ---

  - metadata:
      name: "releng_frontend"
      description: "Test, build and deploy releng_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/releng_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-releng_frontend-*
      - assume:hook-id:project-releng/services-production-releng_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-mapper (master) ---

  - metadata:
      name: "mozilla-releng-mapper"
      description: "Test, build and deploy mozilla-releng-mapper"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_mapper"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-releng-mapper-*
      - assume:hook-id:project-releng/services-master-mozilla-releng-mapper-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-mapper"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-mapper (staging) ---

  - metadata:
      name: "mozilla-releng-mapper"
      description: "Test, build and deploy mozilla-releng-mapper"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_mapper"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-releng-mapper-*
      - assume:hook-id:project-releng/services-staging-mozilla-releng-mapper-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-mapper"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-tooltool (master) ---

  - metadata:
      name: "mozilla-releng-tooltool"
      description: "Test, build and deploy mozilla-releng-tooltool"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_tooltool"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-releng-tooltool-*
      - assume:hook-id:project-releng/services-master-mozilla-releng-tooltool-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-tooltool"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-tooltool (staging) ---

  - metadata:
      name: "mozilla-releng-tooltool"
      description: "Test, build and deploy mozilla-releng-tooltool"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_tooltool"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-releng-tooltool-*
      - assume:hook-id:project-releng/services-staging-mozilla-releng-tooltool-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-tooltool"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-treestatus (master) ---

  - metadata:
      name: "mozilla-releng-treestatus"
      description: "Test, build and deploy mozilla-releng-treestatus"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_treestatus"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-releng-treestatus-*
      - assume:hook-id:project-releng/services-master-mozilla-releng-treestatus-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-treestatus"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-treestatus (staging) ---

  - metadata:
      name: "mozilla-releng-treestatus"
      description: "Test, build and deploy mozilla-releng-treestatus"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_treestatus"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-releng-treestatus-*
      - assume:hook-id:project-releng/services-staging-mozilla-releng-treestatus-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-treestatus"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-releng-treestatus (production) ---

  - metadata:
      name: "mozilla-releng-treestatus"
      description: "Test, build and deploy mozilla-releng-treestatus"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/releng_treestatus"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-mozilla-releng-treestatus-*
      - assume:hook-id:project-releng/services-production-mozilla-releng-treestatus-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-releng-treestatus"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-bot-uplift (master) ---

  - metadata:
      name: "mozilla-shipit-bot-uplift"
      description: "Test, build and deploy mozilla-shipit-bot-uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_bot_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-shipit-bot-uplift-*
      - assume:hook-id:project-releng/services-master-mozilla-shipit-bot-uplift-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-bot-uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-bot-uplift (staging) ---

  - metadata:
      name: "mozilla-shipit-bot-uplift"
      description: "Test, build and deploy mozilla-shipit-bot-uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_bot_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-shipit-bot-uplift-*
      - assume:hook-id:project-releng/services-staging-mozilla-shipit-bot-uplift-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-bot-uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-bot-uplift (production) ---

  - metadata:
      name: "mozilla-shipit-bot-uplift"
      description: "Test, build and deploy mozilla-shipit-bot-uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/shipit_bot_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-mozilla-shipit-bot-uplift-*
      - assume:hook-id:project-releng/services-production-mozilla-shipit-bot-uplift-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-bot-uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_frontend (master) ---

  - metadata:
      name: "shipit_frontend"
      description: "Test, build and deploy shipit_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-shipit_frontend-*
      - assume:hook-id:project-releng/services-master-shipit_frontend-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_frontend (staging) ---

  - metadata:
      name: "shipit_frontend"
      description: "Test, build and deploy shipit_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-shipit_frontend-*
      - assume:hook-id:project-releng/services-staging-shipit_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_frontend (production) ---

  - metadata:
      name: "shipit_frontend"
      description: "Test, build and deploy shipit_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/shipit_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-shipit_frontend-*
      - assume:hook-id:project-releng/services-production-shipit_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-pipeline (master) ---

  - metadata:
      name: "mozilla-shipit-pipeline"
      description: "Test, build and deploy mozilla-shipit-pipeline"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_pipeline"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-shipit-pipeline-*
      - assume:hook-id:project-releng/services-master-mozilla-shipit-pipeline-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-pipeline"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-pipeline (staging) ---

  - metadata:
      name: "mozilla-shipit-pipeline"
      description: "Test, build and deploy mozilla-shipit-pipeline"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_pipeline"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-shipit-pipeline-*
      - assume:hook-id:project-releng/services-staging-mozilla-shipit-pipeline-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-pipeline"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-signoff (master) ---

  - metadata:
      name: "mozilla-shipit-signoff"
      description: "Test, build and deploy mozilla-shipit-signoff"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_signoff"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-shipit-signoff-*
      - assume:hook-id:project-releng/services-master-mozilla-shipit-signoff-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-signoff"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-signoff (staging) ---

  - metadata:
      name: "mozilla-shipit-signoff"
      description: "Test, build and deploy mozilla-shipit-signoff"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_signoff"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-shipit-signoff-*
      - assume:hook-id:project-releng/services-staging-mozilla-shipit-signoff-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-signoff"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-uplift (master) ---

  - metadata:
      name: "mozilla-shipit-uplift"
      description: "Test, build and deploy mozilla-shipit-uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-mozilla-shipit-uplift-*
      - assume:hook-id:project-releng/services-master-mozilla-shipit-uplift-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-uplift (staging) ---

  - metadata:
      name: "mozilla-shipit-uplift"
      description: "Test, build and deploy mozilla-shipit-uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-mozilla-shipit-uplift-*
      - assume:hook-id:project-releng/services-staging-mozilla-shipit-uplift-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- mozilla-shipit-uplift (production) ---

  - metadata:
      name: "mozilla-shipit-uplift"
      description: "Test, build and deploy mozilla-shipit-uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/shipit_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-mozilla-shipit-uplift-*
      - assume:hook-id:project-releng/services-production-mozilla-shipit-uplift-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "mozilla-shipit-uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"

