version: 0
allowPullRequests: public
metadata:
  name: "Mozilla RelEng"
  description: "Mozilla RelEng Services"
  owner: "{{ event.head.user.email }}"
  source: "{{ event.head.repo.url }}"
tasks:

# --- elm_common_example (master) ---

  - metadata:
      name: "elm_common_example"
      description: "Test, build and deploy elm_common_example"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/lib/elm_common/example"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-elm_common_example-*
      - assume:hook-id:project-releng/services-master-elm_common_example-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "elm_common_example"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- elm_common_example (staging) ---

  - metadata:
      name: "elm_common_example"
      description: "Test, build and deploy elm_common_example"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/lib/elm_common/example"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-elm_common_example-*
      - assume:hook-id:project-releng/services-staging-elm_common_example-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "elm_common_example"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_archiver (master) ---

  - metadata:
      name: "releng_archiver"
      description: "Test, build and deploy releng_archiver"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_archiver"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_archiver-*
      - assume:hook-id:project-releng/services-master-releng_archiver-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_archiver"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_archiver (staging) ---

  - metadata:
      name: "releng_archiver"
      description: "Test, build and deploy releng_archiver"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_archiver"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_archiver-*
      - assume:hook-id:project-releng/services-staging-releng_archiver-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_archiver"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_clobberer (master) ---

  - metadata:
      name: "releng_clobberer"
      description: "Test, build and deploy releng_clobberer"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_clobberer"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_clobberer-*
      - assume:hook-id:project-releng/services-master-releng_clobberer-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_clobberer"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_clobberer (staging) ---

  - metadata:
      name: "releng_clobberer"
      description: "Test, build and deploy releng_clobberer"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_clobberer"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_clobberer-*
      - assume:hook-id:project-releng/services-staging-releng_clobberer-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_clobberer"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_docs (master) ---

  - metadata:
      name: "releng_docs"
      description: "Test, build and deploy releng_docs"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_docs"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_docs-*
      - assume:hook-id:project-releng/services-master-releng_docs-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_docs"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_docs (staging) ---

  - metadata:
      name: "releng_docs"
      description: "Test, build and deploy releng_docs"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_docs"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_docs-*
      - assume:hook-id:project-releng/services-staging-releng_docs-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_docs"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_docs (production) ---

  - metadata:
      name: "releng_docs"
      description: "Test, build and deploy releng_docs"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/releng_docs"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-releng_docs-*
      - assume:hook-id:project-releng/services-production-releng_docs-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_docs"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_frontend (master) ---

  - metadata:
      name: "releng_frontend"
      description: "Test, build and deploy releng_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_frontend-*
      - assume:hook-id:project-releng/services-master-releng_frontend-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_frontend (staging) ---

  - metadata:
      name: "releng_frontend"
      description: "Test, build and deploy releng_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_frontend-*
      - assume:hook-id:project-releng/services-staging-releng_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_frontend (production) ---

  - metadata:
      name: "releng_frontend"
      description: "Test, build and deploy releng_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/releng_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-releng_frontend-*
      - assume:hook-id:project-releng/services-production-releng_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_mapper (master) ---

  - metadata:
      name: "releng_mapper"
      description: "Test, build and deploy releng_mapper"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_mapper"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_mapper-*
      - assume:hook-id:project-releng/services-master-releng_mapper-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_mapper"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_mapper (staging) ---

  - metadata:
      name: "releng_mapper"
      description: "Test, build and deploy releng_mapper"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_mapper"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_mapper-*
      - assume:hook-id:project-releng/services-staging-releng_mapper-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_mapper"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_tooltool (master) ---

  - metadata:
      name: "releng_tooltool"
      description: "Test, build and deploy releng_tooltool"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_tooltool"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_tooltool-*
      - assume:hook-id:project-releng/services-master-releng_tooltool-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_tooltool"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_tooltool (staging) ---

  - metadata:
      name: "releng_tooltool"
      description: "Test, build and deploy releng_tooltool"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_tooltool"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_tooltool-*
      - assume:hook-id:project-releng/services-staging-releng_tooltool-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_tooltool"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_treestatus (master) ---

  - metadata:
      name: "releng_treestatus"
      description: "Test, build and deploy releng_treestatus"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/releng_treestatus"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-releng_treestatus-*
      - assume:hook-id:project-releng/services-master-releng_treestatus-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_treestatus"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_treestatus (staging) ---

  - metadata:
      name: "releng_treestatus"
      description: "Test, build and deploy releng_treestatus"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/releng_treestatus"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-releng_treestatus-*
      - assume:hook-id:project-releng/services-staging-releng_treestatus-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_treestatus"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- releng_treestatus (production) ---

  - metadata:
      name: "releng_treestatus"
      description: "Test, build and deploy releng_treestatus"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/releng_treestatus"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-releng_treestatus-*
      - assume:hook-id:project-releng/services-production-releng_treestatus-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "releng_treestatus"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_bot_uplift (master) ---

  - metadata:
      name: "shipit_bot_uplift"
      description: "Test, build and deploy shipit_bot_uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_bot_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-shipit_bot_uplift-*
      - assume:hook-id:project-releng/services-master-shipit_bot_uplift-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_bot_uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_bot_uplift (staging) ---

  - metadata:
      name: "shipit_bot_uplift"
      description: "Test, build and deploy shipit_bot_uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_bot_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-shipit_bot_uplift-*
      - assume:hook-id:project-releng/services-staging-shipit_bot_uplift-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_bot_uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_bot_uplift (production) ---

  - metadata:
      name: "shipit_bot_uplift"
      description: "Test, build and deploy shipit_bot_uplift"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/shipit_bot_uplift"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-shipit_bot_uplift-*
      - assume:hook-id:project-releng/services-production-shipit_bot_uplift-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_bot_uplift"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_dashboard (master) ---

  - metadata:
      name: "shipit_dashboard"
      description: "Test, build and deploy shipit_dashboard"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_dashboard"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-shipit_dashboard-*
      - assume:hook-id:project-releng/services-master-shipit_dashboard-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_dashboard"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_dashboard (staging) ---

  - metadata:
      name: "shipit_dashboard"
      description: "Test, build and deploy shipit_dashboard"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_dashboard"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-shipit_dashboard-*
      - assume:hook-id:project-releng/services-staging-shipit_dashboard-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_dashboard"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_dashboard (production) ---

  - metadata:
      name: "shipit_dashboard"
      description: "Test, build and deploy shipit_dashboard"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/shipit_dashboard"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-shipit_dashboard-*
      - assume:hook-id:project-releng/services-production-shipit_dashboard-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_dashboard"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_frontend (master) ---

  - metadata:
      name: "shipit_frontend"
      description: "Test, build and deploy shipit_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-shipit_frontend-*
      - assume:hook-id:project-releng/services-master-shipit_frontend-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_frontend (staging) ---

  - metadata:
      name: "shipit_frontend"
      description: "Test, build and deploy shipit_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-shipit_frontend-*
      - assume:hook-id:project-releng/services-staging-shipit_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_frontend (production) ---

  - metadata:
      name: "shipit_frontend"
      description: "Test, build and deploy shipit_frontend"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/production/src/shipit_frontend"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:production
      - hooks:modify-hook:project-releng/services-production-shipit_frontend-*
      - assume:hook-id:project-releng/services-production-shipit_frontend-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - production
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_frontend"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:production"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_pipeline (master) ---

  - metadata:
      name: "shipit_pipeline"
      description: "Test, build and deploy shipit_pipeline"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_pipeline"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-shipit_pipeline-*
      - assume:hook-id:project-releng/services-master-shipit_pipeline-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_pipeline"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_pipeline (staging) ---

  - metadata:
      name: "shipit_pipeline"
      description: "Test, build and deploy shipit_pipeline"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_pipeline"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-shipit_pipeline-*
      - assume:hook-id:project-releng/services-staging-shipit_pipeline-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_pipeline"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_signoff (master) ---

  - metadata:
      name: "shipit_signoff"
      description: "Test, build and deploy shipit_signoff"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/master/src/shipit_signoff"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:master
      - hooks:modify-hook:project-releng/services-master-shipit_signoff-*
      - assume:hook-id:project-releng/services-master-shipit_signoff-*
    extra:
      github:
        env: true
        events:
          - pull_request.*
          - push
        branches:
          - master
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_signoff"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:master"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"


# --- shipit_signoff (staging) ---

  - metadata:
      name: "shipit_signoff"
      description: "Test, build and deploy shipit_signoff"
      owner: "{{ event.head.user.email }}"
      source: "https://github.com/mozilla-releng/services/tree/staging/src/shipit_signoff"
    scopes:
      - secrets:get:repo:github.com/mozilla-releng/services:branch:staging
      - hooks:modify-hook:project-releng/services-staging-shipit_signoff-*
      - assume:hook-id:project-releng/services-staging-shipit_signoff-*
    extra:
      github:
        env: true
        events:
          - push
        branches:
          - staging
    provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    payload:
      maxRunTime: 7200 # seconds (i.e. two hours)
      image: "nixos/nix:1.11"
      features:
        taskclusterProxy: true
      env:
        APP: "shipit_signoff"
        TASKCLUSTER_SECRETS: "taskcluster/secrets/v1/secret/repo:github.com/mozilla-releng/services:branch:staging"
      command:
        - "/bin/bash"
        - "-c"
        - "nix-env -iA nixpkgs.gnumake nixpkgs.curl nixpkgs.cacert && export SSL_CERT_FILE=$HOME/.nix-profile/etc/ssl/certs/ca-bundle.crt && mkdir /src && cd /src && curl -L https://github.com/mozilla-releng/services/archive/$GITHUB_HEAD_SHA.tar.gz -o $GITHUB_HEAD_SHA.tar.gz && tar zxf $GITHUB_HEAD_SHA.tar.gz && cd services-$GITHUB_HEAD_SHA && ./.taskcluster.sh"

