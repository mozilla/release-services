- CSRF tokens for POSTs, check 
- implement the clobberer user API so we have something to play with
- auth (oAuth2?)
- simple proxying support
- scheduled tasks

Minor
- allow register_model to take three args
- log instead of printing at startup
- allow config to be specified on cmdline, with defaults
- register tables with lambdas?  db.table(bp, 'dbbame', lambda metadata: ..)
  .. this allows LocalProxy!

Scripts
- move base scripts to blueprint

Config
- config handling - subcommand to generate an example config file

Docs
- doc how to access a table
- fix DB config docs
- finish docs w/ decent UI
- docs should render rst?  Or just sphinx and rtd?
  - include dynamic link to or generation of table structure?
