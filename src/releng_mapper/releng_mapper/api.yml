---
swagger: "2.0"
info:
  version: "1.0.0"
  title: "ShipIt Workflow"
consumes:
  - application/json
produces:
  - application/json
paths:

  /example:
    get:
      operationId: "releng_mapper.api.example"
      responses:
        200:
          description: Example
